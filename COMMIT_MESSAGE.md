Global Trend — Task Management App

- Implement Express backend with SQLite persistence
- Frontend: Responsive HTML/CSS/vanilla JS with task list, add/edit form
- Features: CRUD tasks, search, status filters, relative timestamps, optimistic delete with undo, toasts
- Added `better-sqlite3` and DB helper `db/sqlite.js`
- Added seed script `npm run seed` to populate sample tasks
- Removed unused Mongo/Docker files to keep project minimal

Run locally:
- `npm install`
- `npm run seed` (optional)
- `npm run dev`

(Generated for submission)