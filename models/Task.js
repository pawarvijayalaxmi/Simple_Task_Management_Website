// Mongoose model removed in favor of a simple JSON file DB.
// The routes now use `data/db.json` for persistence so the app
// can run without installing MongoDB. This file is kept to
// indicate the previous structure and may be deleted.

module.exports = {};
